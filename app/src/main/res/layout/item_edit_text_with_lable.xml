<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvCommonLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/outfit_regular"
        android:textSize="18sp"
        tools:text="@string/first_name"
        android:layout_marginVertical="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/etCommonEditText"
        />
    <androidx.appcompat.widget.AppCompatEditText
        android:id="@+id/etCommonEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:hint="@string/first_name"
        android:textSize="18sp"
        android:layout_marginTop="8dp"
        android:inputType="text"
        android:padding="15dp"
        android:fontFamily="@font/outfit_regular"
        android:background="@drawable/bg_with_radius"
        android:layout_marginBottom="10dp"
        app:layout_constraintTop_toBottomOf="@id/tvCommonLabel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>