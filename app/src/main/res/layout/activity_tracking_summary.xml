<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ebb"
    tools:context=".trackingSummary.TrackingSummary">
    <include
        android:id="@+id/TrackingSummaryHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        layout="@layout/toolbar_tracking_summary"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTrackingSummary"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btnStartTracking"
        tools:listitem="@layout/item_tracking_summary"
        app:layout_constraintTop_toBottomOf="@id/TrackingSummaryHeader"
        />
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnStartTracking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="40dp"
        android:background="@drawable/bg_with_radius"
        android:text="@string/start_tracking"
        android:fontFamily="@font/outfit_semi_bold"
        android:textColor="@color/white"
        android:textAllCaps="false"
        android:paddingHorizontal="70dp"
        android:paddingVertical="20dp"
        android:textSize="14sp"
        android:backgroundTint="@color/science_blue"
        app:layout_constraintTop_toBottomOf="@id/rvTrackingSummary"
        app:layout_constraintBottom_toBottomOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>